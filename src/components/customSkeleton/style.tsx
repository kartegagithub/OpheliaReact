import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  questionBox: {
    width: '100%',
    height: 290,
    borderRadius: 16,
    backgroundColor: 'white',
    marginBottom: 16,
  },
});
