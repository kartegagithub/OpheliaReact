import {StyleSheet} from 'react-native';

const iconScreenStyle = StyleSheet.create({
  iconArea: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});

export default iconScreenStyle;
