import {StyleSheet} from 'react-native';

const linearGradientScreenStyle = StyleSheet.create({
  button: {
    padding: 15,
    borderRadius: 7,
  },
  buttonText: {
    color: '#fff',
    fontSize: 16,
    textAlign: 'center',
  },
});

export default linearGradientScreenStyle;
